<!-- res/values/themes.xml -->
<resources>

    <!-- 🌟 Tema base Aureum (solo una versión, limpia y completa) -->
    <style name="Theme.Aureum" parent="Theme.Material3.Light.NoActionBar">
        <!-- Barra de estado -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>

        <!-- 🎨 Paleta Aureum (sin morado) -->
        <item name="colorPrimary">@color/aureum_gold</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/aureum_gold</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="colorTertiary">@color/aureum_cream_dark</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/aureum_text</item>
        <item name="android:colorBackground">@color/white</item>

        <!-- 🔧 Evita el tinte morado predeterminado de Material3 -->
        <item name="materialButtonStyle">@style/PlainButton</item>
        <item name="buttonStyle">@style/PlainButton</item>

        <!-- Formas redondeadas -->
        <item name="shapeAppearanceSmallComponent">@style/Shape.Rounded</item>
        <item name="shapeAppearanceMediumComponent">@style/Shape.Rounded</item>
        <item name="shapeAppearanceLargeComponent">@style/Shape.Rounded</item>

        <!-- Overlay (mantén si lo usas) -->
        <item name="materialThemeOverlay">@style/ThemeOverlay.Aureum</item>
    </style>

    <!-- Overlay sin fuente (opcional, puedes añadir tipografía aquí) -->
    <style name="ThemeOverlay.Aureum" parent="">
        <!-- <item name="fontFamily">@font/poppins_regular</item> -->
    </style>

    <!-- Formas redondeadas -->
    <style name="Shape.Rounded" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">24dp</item>
    </style>

</resources>
